# 第三轮代码复核与修复建议

本轮以当前磁盘源码为准，重新执行 Debug/x64 构建，退出码 0，构建通过。输出隔离至 audit-third，日志为 audit-third-build.log。未修改业务代码，未启动生产程序或访问设备。本轮是对修复声明、关键调用链及补充缺陷的复核，不是重复完成一次逐行全库审核。

## 对朋友分析的判断

“不要在不了解硬件时序时直接大改，先验证已有修改”是合理建议。但“只剩 R4/R5/R6/R8/R9，其他都彻底消除”不符合当前代码；“这些问题都只能等现场信息”也过于笼统。构建已通过，下一步应先补离线故障注入和并发测试，再受控联机验证，不能以正常联机运行一段时间代替异常路径验证。

### R1：本轮已修复所指出的判定不一致

Form1.cs:4228–4233 在 faultThisFrame 时设置 tempout1/tempout2 为 Reject、cuowu1 为 NG。上一轮“发999但旧Accept计OK/存OK”的具体缺口已补上。此结论是源码确认，尚未验证 VPP 和 PLC 是否把999作为故障码。

### R2：只修复了直接抛异常的捕获，未完全闭合

Form1.cs:4042–4046 已捕获 Run 直接抛出的异常并设置 runError，这是正确修复。

但 Form1.cs:3902、3922/3928 设置 inputAssignFailed 后，4030 仍无条件执行 myjob.block.Run()。输入无效仍运行旧图，故障结果只能在 Run 返回后发布；若无效输入让流程长时间执行，故障应答也被拖延。不能宣称“传图失败跳过检测”已完成。

建议：输入校验失败时直接进入统一故障结果处理，跳过 Run；保留 finally 清理。该控制流可离线注入传图失败和 Run 异常验证，不需要现场协议才能写出正确分支。

### R3：修了文本框串值，但队列顺序与 CSV 快照仍未闭合

Form3.changeok 的文本发送已用 aa 替代 textBox.Text（1653，服务端同类分支也修改），这一点应认可。

但 Form1 与 Form3 仍多重 Task.Run，没有按帧/连接顺序消费；正确的 A、B 快照仍可能按 B、A 发送。Form1.cs:5054–5077 仍在后台读取 myjob.block.Outputs 中的 ji 字段，并修改共享 myjob.tianbiao，CSV 可能记录后续帧的数据。4588 等处的 oksum++ 仍在可并发的任务中执行，计数也未串行化。

建议：通信请求按连接串行处理，CSV 使用局部不可变快照，计数在相机所属线程中更新。可以用两个有明确执行顺序的测试事件控制 A、B 任务延迟离线验证，不必先连 PLC。

### R7：假成功显示已修复，但失败结果处理未闭合

FormModbusRtu.cs:2383–2396 等处已根据 Write 的实际结果显示已发送/发送失败；string 分支也聚合结果。

但 2493 仍无条件把 pending 清为“无”，接口未将失败传给主检测流程。这意味着“错误回显”修复了，“每帧结果可靠送达或明确故障上报”还没有完成。是否重试、是否允许丢弃需要生产要求；不能未经设计无限重试旧帧。

建议：明确返回失败/部分成功状态并记录相机、帧号、地址和错误原因。可以使用失败的模拟通信适配器离线验证，再确定现场超时和重试策略。

## 剩余五项怎样处理

### R4：请求队列是合理终点，但可以分协议推进

当前 [4]/[5] 单槽与清标志仍有旧任务清掉新请求的问题，读值改为 pat.Value[4] 不能解决所有权。至少要使登记、消费和清除属于同一同步协议；进一步引入带相机号、帧号、地址和值的队列。可先处理实际启用的协议，不要求一次把全部协议一起重写。

现场需要确认的是：每件必检还是只保留最新状态、积压上限、过期结果和断线策略。锁/队列的并发正确性可以用模拟写入器离线验证。

### R5：确实不应直接盲改 Shutdown，但不是只能等待硬件

排空超时后仍继续关闭是已知缺陷。最小安全方向是把关闭/切换作为可失败的操作，让失败一路返回调用者，保持活动对象，不继续重载。退出时还须决定取消退出、提示等待或明确强制退出的流程，不能只在内部加一个 return 而让外层继续销毁。

完成信号要覆盖回调、检测及仍使用方案对象的任务；保存也要等完成，不能用 Sleep(300) 当作同步。用阻塞的假检测任务可验证“超时绝不 Shutdown”和“未排空不能保存”；真实 SDK 停采集、关闭时延再联机验证。

补充路径：Form1.cs:17485–17494 以及其他相机子流程选择，仅检查 !yunxing 就直接更换 myjob.block。刚停止时旧检测仍可能在途，主方案切换修好后也不能漏掉这条入口。

### R6：硬件脉宽需实测，软件排空并不需要等实测才能修

入队原子性已修复。剩余 UI Timer 与 FlushIoWork 问题应分别处理：

- Flush 应等待“队列空且执行中任务已完成”，可用完成计数/屏障做确定性测试。
- 脉冲状态和计时应由专用执行器维护，实际置高成功后计时；简单更换成线程池 Timer 并不能保证精确脉宽，还需要避免重入和晚到关断。
- 所需脉宽精度、硬件支持与最终波形必须现场确认。

WinForms Timer 的处理依赖 UI 线程，不能把UI阻塞下的准时关断当作保证。[Microsoft Timer 文档](https://learn.microsoft.com/en-us/dotnet/api/system.windows.forms.timer)

### R8 与 R9 必须分开

R8 是自定义实现的标准 Modbus RTU：功能码03/06/10、字节数、CRC和异常响应已有标准。CRC与帧结构不需要现场重新定义；需要现场确认的是端口参数、设备超时、站号、寄存器业务含义及可能的非标准行为。可先用标准报文向量、短帧、分段回包、错误CRC和异常响应做离线测试。按实读长度处理是一项局部修复，不能称为已经完整组帧，也不能把任何修改笼统称作“零风险”。[Modbus 官方协议与实现指南](https://www.modbus.org/modbus-specifications)

R9 是自定义 TCP 命令：确实需要确认长度头、结束符或固定长度。不能擅自加换行来适配现场；可先从配置、已有协议文档或抓包确认边界，再编写每连接解析器。

## 其他问题与修复数量核对

### 新增发现 [P2] 曲线快速停止再启动会保留旧轮询线程

位置：WindowsFormsApplication1/DemoControl/UserControlCurve.cs:59–67、76–86。

启动每次创建新 Thread；停止只把共享 isThreadRun 置 false，没有等待旧线程结束。如果旧线程还在 Sleep 时再次启动，标志又变 true，新线程启动；旧线程醒来后读取PLC，并继续循环。因此同一控件可能出现多个采样线程，产生重复读数和额外PLC请求。

这是独立于上一轮“非正数间隔”检查的生命周期问题；正数校验不会解决。建议每轮使用独立取消标志/令牌，并确保旧线程完成后再允许启动；不能在UI线程阻塞等待一个正在 Invoke UI 的线程，以免死锁。可用模拟 ReadWriteNet 和受控等待离线验证。

### 原报告还有明确保留的代码，不支持“只剩五项”的结论

- Form1.cs:11591、11602 的 Bitmap stride 仍是宽度或宽度×3，原对齐问题未改。
- Form1.cs:4427 曝光补偿表达式仍未改变；需先确认 buchang 含义。
- Form9.cs:176–179 仍只替换 Inspect1 与窗体字典，未替换实际检测工具容器中的工具。
- 连续存图仍使用秒级时间与 JobNumber 构成文件名，原同名问题未闭合。
- Form5 的默认/明文口令方案仍存在；应按本机权限边界评估。

因此不能认可“12项彻底消除+4项完全闭合=只余五项”这一汇总。原20项与后续R编号不是一一对应且互不重叠的集合，不能直接相加。应逐条记录具体子问题、代码证据和测试证据。

## 实际建议

先补不依赖硬件的改动：输入失败跳过Run、CSV结果快照、计数串行、采样线程生命周期、发送失败向上传递、Flush完成屏障以及标准RTU解析测试。并行设计 R4/R5/R6 的状态和退出契约，在离线可重复验证后逐个接入真实 SDK。

再进行受控联机回归：交替OK/NG、故障999、短帧、断线、并发触发、超过5秒的检测、切换/关闭和UI卡顿下的脉宽。确认普通正常路径没有回归很有价值，但不能证明低概率竞态和异常路径已经安全。
