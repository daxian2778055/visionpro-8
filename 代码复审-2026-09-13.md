# 修改后代码复审

日期：2026-09-13。对象：当前解决方案实际编译的源码。本次只审查，不修改业务代码。上一轮报告保留作为历史记录，当前结论以本报告为准。

## 验证结果

本次 Debug/x64 **构建通过，MSBuild 退出码 0**，产物隔离在 audit-recheck/bin，完整日志为 audit-recheck-build.log。仍有未使用变量等编译警告；构建通过不等于检测结果和现场时序已验收。

重新读取了修改后的主检测、TCP/串口、ModbusTCP/RTU、FINS、参数设置与辅助控件实现，并逐项复核上轮 20 项问题。未连接相机或 PLC，未启动检测程序。以下并发交错为源码分析，未宣称硬件复现。

上轮问题复核：6 项原缺陷在源码层面已消除，8 项部分修复，6 项仍保留。这里的“已消除”仅对应原报告的具体缺陷，不是整个模块通过验收。

## 当前优先问题

### R1 [P1] 故障帧发 999/NG，但仍可能计 OK、存入 OK 文件夹

位置：WindowsFormsApplication1/Form1.cs:4075–4078、4222–4247、4570、5051、5209。

新代码已将 faultThisFrame 用于协议数据 999 和 NG 脉冲，这是有效改进。但没有在 faultThisFrame 为真时把 tempout1/tempout2 统一设置为 Reject。4570 的注释称“故障帧=Reject”，实际变量仍来自 ToolBlock 的 Output（除独立的耗时判定外未被覆盖）。

触发示例：前一帧 Accept；本帧脚本在更新 Output 前报 Error，或传图失败后使用旧输入运行。通信可能发 999、IO 发 NG，同时合格数加一，CSV 按 OK 记录，图片进入 OK 分支。

建议：构造唯一的最终结果对象，先统一故障、超时和正常判定，再供通信、计数、CSV、显示及存图消费；不要只修改发送内容。

### R2 [P1] 传图失败仍执行 Run；Run 抛异常会绕过故障应答

位置：WindowsFormsApplication1/Form1.cs:3899–3903、3917–3928、4028–4042、5426–5430。

inputAssignFailed 已登记，但 4030 仍无条件执行 myjob.block.Run()。传图失败时仍可能执行旧图或无效输入。Run 只有 try/finally，没有本地 catch；直接抛出异常时会跳到 getrecord 最外层，只释放 Bitmap、记录日志，无法到达 4222 后的新 999/NG 路径。

建议：输入无效时跳过 Run；Run 抛异常与 RunStatus.Error 都转换为本帧统一故障结果。验证缺失输入、格式错误、脚本抛异常三种路径都能产生规定应答。

### R3 [P1] 上层快照仍被 TCP 下层文本框覆盖，发送顺序也未固定

位置：WindowsFormsApplication1/Form3.cs:1646–1671、1696–1715；WindowsFormsApplication1/Form1.cs:4259–4272、5042–5076。

Form1 的 snapTcp/snapSerial/snapMtcp 已避免部分后台读取 ToolBlock，这是有效修复。但 changeok 把 aa 写进 textBox8 后，又在 Task.Run 内读取 textBox8.Text；服务端分支也读取 textBox2.Text。连续提交 A、B，若 A 的任务晚执行，两次可能都发送 B。此问题适用于文本发送路径，二进制分支读取 aa 的行为不同。

外层与内层多重 Task.Run 仍无帧序保证。CSV 的 ji 输出仍在后台读取 myjob.block.Outputs，未纳入快照；同一路计数任务并发自增也没有串行化。

建议：changeok 直接使用不可变参数，按连接排队发送；将 ji 值、最终判定、帧号及方案标识一起快照，计数在所属相机流程内串行更新。

### R4 [P1] 跨相机取值已改正，但共享 pending 槽仍会丢结果或重复发送

位置：WindowsFormsApplication1/Form1.cs:4345–4348、4386–4389；WindowsFormsApplication1/FormModbus.cs:1656–1663、1507；WindowsFormsApplication1/FormOmron.cs:1146–1160、1214；WindowsFormsApplication1/FormModbusRtu.cs:2366–2379、2484。

各协议已将 value.Split 改为 pat.Value[4].Split，因此上一轮“把 A 值直接写到 B 地址”的具体错误已修正。但 [4]、[5] 仍是无完整事务保护的单槽结果。旧发送读取 A 后，新帧登记 B 和 pending，随后旧发送把 [5] 清为“无”，新任务遍历时就看不到 B。FINS/RTU 还可能有多个遍历同时消费同一 pending。

ModbusTCP xie 的锁不能覆盖锁外的 WriteCameraResult 登记。此条针对全表 pending 发送路径；默认 XieWuWriteOne 按相机直写不能不加区分地套用同一结论。

建议：用带相机号、帧号、目标地址和值的请求队列；消费一个确定的请求，避免“处理旧请求时清除新请求”。

### R5 [P1] 排空超时仍执行 Shutdown，保存也仍以固定延时代替完成等待

位置：WindowsFormsApplication1/Form1.cs:9315–9319、11084–11097、11956–11958、8271–8276。

WaitDetectDrain 增加 _detectBusy 检查，比以前只检查 Run 计数更完整。但超时返回 false 后只写告警，仍继续 manager1.Shutdown；检测超过 5 秒时依旧可并发关闭活动方案。保存仍 Sleep(300) 后直接序列化，未等待完整检测结束。

已确认 12011–12017 新增 _bmpInFlight 判断，会跳过在途 HGlobal 的释放，因此不再沿用上一轮“关闭无条件释放在途缓冲”的原结论。但这一局部保护不能保证 ToolBlock 生命周期安全。WaitDetectDrain 也不跟踪 Task.Run 的输出/存图工作，方案重载仍可能与旧任务交错。

建议：超时必须中止销毁/保存操作并保留对象；统一完成信号覆盖采集回调、检测、必要的结果任务。不要把超时日志视为互斥措施。

### R6 [P1] IO 入队反序已修复，但脉冲关断仍依赖界面

位置：WindowsFormsApplication1/Form1.cs:5766–5792、5803–5805、5750–5756。

开启、到期关断和停止关断均已在 _ioPulseLock 内入队，原“关断先入队、置高后入队”的竞态已消除。

但关断仍依赖 WinForms Timer，UI 阻塞时无法按时关断；截止时间在实际硬件置高前计算，IO 队列延迟会缩短实际脉冲。FlushIoWork 仅等队列 Count 为 0；工作线程取走最后一项但尚未执行完时，就可能提前返回，后续关闭设备会与最后一次关断交错。

建议：IO 状态与计时由专用执行器或硬件控制，置高成功后计时；排空检查必须包含正在执行的最后一项，不能只检查队列长度。

### R7 [P1] RTU 写失败仍显示“已发送”并清除待发结果

位置：WindowsFormsApplication1/FormModbusRtu.cs:2383–2395、2409–2421、2435–2447、2484。

RTU xie_wu 仍丢弃 Write 的返回结果，随后更新“已发送”，最后清除 pending。断线或 PLC 拒绝写入时仍可能假报成功。

ModbusTCP 的逐寄存器降级已聚合失败结果（1444–1452），该部分修复有效；不能因此将 RTU 同类路径一并关闭。

建议：按实际写入结果更新状态，错误返回生产流程；明确重试和过期结果处理策略。

### R8 [P1] 自定义 Modbus 只修正读取长度，仍未完整组帧/校验

位置：WindowsFormsApplication1/Modbus.cs:28–43、55–67。

按 buffer.Length 解析已改成 r/n，避免读取未更新的尾部，这是有效改进。但收到部分报文后仍立即解析并 DiscardInBuffer，没有累积到 expectCount；写回执仍不校验功能码、地址/数据及 CRC。首轮解析会去头尾，后续匹配分支却将头尾全部拼入 b，同一响应在不同路径会返回不同格式。

建议：实现完整事务、有限超时组帧和校验，或统一使用项目已有协议库；用分段回包、异常响应和错误 CRC 验证。

### R9 [P1] TCP 半包/粘包问题仍存在

位置：WindowsFormsApplication1/Form3.cs:626–638、829–843。

服务端接收缓冲已改为每连接局部变量，原多连接覆盖问题已修复；但两端仍直接把一次 Receive 当成完整触发命令，未缓存、分帧。一个命令拆成两次接收或多个命令合并接收，仍可能漏触发。

建议：按现场协议明确报文边界，保留未完整的数据，并循环拆出完整命令。

## 仍保留的 P2 项目

- **Bitmap 步长未对齐**：Form1.cs:11579、11590 仍直接传宽度或宽度×3。非 4 倍数宽度的原构造异常未修复，是否能由现场 ROI 触发取决于设备宽度增量约束。上轮独立 API 验证仍适用于未改变的构造表达式，本轮未重复运行该验证。
- **曝光公式未修正/澄清**：Form1.cs:4415 仍为 `曝光 * (255 - buchang / 255)`。快照只解决读取时机，未改变数量级；buchang=255 时因子为254。需核对 VPP 输出含义。
- **模板加载未进入实际工具树**：Form9.cs:176–179 仍只更新 Inspect1 与窗体字典，未替换 block4/嵌套容器内的实际工具。
- **连续存图同名**：Form1.cs:15038–15046 仍使用秒级时间与不保证每帧递增的 JobNumber，连续存图同一秒可能覆盖。
- **口令边界未变**：Form5.cs:58、78 仍使用 INI 明文与默认口令；按上轮说明评估本地配置写权限和厂家权限边界，未据此宣称远程漏洞。

## 上轮 20 项对照

1. 01 编译/long 写入：原缺陷已修复，构建通过。
2. 02 传图失败继续执行：部分修复，见 R1/R2。
3. 03 Error 后旧输出：部分修复，协议/IO 有故障路径，但统计判定和抛异常路径未闭合，见 R1/R2。
4. 04 异步串帧：部分修复，上游已有快照，下游文本框、CSV 与发送顺序仍有问题，见 R3。
5. 05 多相机写错值：已改按相机取值，但 pending 并发仍未解决，整体部分修复，见 R4。
6. 06 生命周期：增加排空与在途缓冲保护，但超时仍 Shutdown，部分修复，见 R5。
7. 07 IO 时序：入队原子性已修复，UI 计时与排空未完整，部分修复，见 R6。
8. 08 共享接收缓冲：原缺陷已修复，Form3.cs:815–816 为局部变量。
9. 09 TCP 分帧：仍保留，见 R9。
10. 10 Modbus 响应：按实际长度解析已改，组帧/校验仍缺，部分修复，见 R8。
11. 11 写失败假成功：TCP 降级路径修复，RTU 保留，部分修复，见 R7。
12. 12 转换失败继续检测：原缺陷已修复，Form1.cs:11526–11530、11555–11559 检查返回值并退出。采集失败直接丢帧是否符合每件必检要求仍需现场确定。
13. 13 RGB 红蓝互换：原直出分支已修复，RGB8 进入转 BGR 路径并同步元数据，未做 SDK 联机验证。
14. 14 stride：仍保留。
15. 15 曝光公式：仍保留，实际目标公式需确认。
16. 16 串口校验位：原缺陷已修复，Form4.cs:69 改读 comboBox5。
17. 17 模板加载：仍保留。
18. 18 存图同名：仍保留。
19. 19 非正数采样间隔：原缺陷已修复，UserControlCurve.cs 在启动前拒绝 timeSleep<=0。
20. 20 口令：仍保留。

## 建议下一步

先处理 R1–R5：确保一帧只有一个最终结果，异常必走故障应答，发送不从控件取值，方案不能在仍被使用时关闭。再处理 IO 和协议组帧/写入确认。回归至少包括交替 OK/NG、脚本直接抛异常、传图失败、同相机连续输出、两相机同时输出、检测超过5秒时切换以及 UI 卡顿时测量脉宽。

这次修改确实修复了多项原缺陷，但当前仍不能仅凭编译通过认定可稳定用于生产。
