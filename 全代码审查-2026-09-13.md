# VisionPro 八相机项目代码审查

审查日期：2026-09-13。对象：本目录 `WindowsFormsApplication1.sln` 实际引用的源码。仅审查，未修改业务代码、相机配置或 PLC 数据。

## 结论与验证边界

当前源码 **Debug/x64 构建失败**。除编译阻断外，存在检测异常沿用旧结果、异步结果串帧、多相机结果写错地址、关闭与检测并发、IO 脉冲时序不可靠等高优先级问题。不能依据已有 bin 中的 EXE 正常运行，认定当前源码可发布。

本次按项目清单做全项目静态扫描，并沿采集→图像转换→ToolBlock 执行→通信/IO→统计/存图及关闭/切换链路做重点人工核查。项目包含 55 个参与编译的 C# 文件，其中 29 个非 Designer/AssemblyInfo 文件；Form1.cs 为 20,652 行，MVCamera.cs 为 4,879 行。其余自动生成文件主要核查编译与事件绑定；不是对每条 Designer 属性作人工逐行证明。

根目录另外的 Form1.cs、FormModbus.cs 以及 `_backup_SQL`、`_backup_catchlog` 不参与本项目编译，不能将这些副本中的问题或修复当成运行版本的事实。第三方 DLL 内部实现、VPP 内嵌脚本、实际相机/PLC 时序不在本次源码验证范围。未启动检测程序、未连接或控制设备。

已执行：

- 使用本机 VS2022 MSBuild 编译整个解决方案，Debug/x64，输出及中间文件隔离至 `audit-build`。编译进入 C# 阶段并报 `FormOmron.cs(1175,57): error CS0103: 当前上下文中不存在名称“vals”`，退出码 1。
- 使用本机 System.Drawing 独立验证项目所用 Bitmap 构造方式：RGB 字节 `255,0,0` 读回 `R=0,G=0,B=255`；Mono8 宽度 4 成功，宽度 5、6 抛出 `Parameter is not valid`。这验证图像转换边界，未验证 SDK 采集。
- 搜索到的 `perf_selftest.cmd` 仅生成模拟性能日志，不是检测结果、通信或长期稳定性测试套件，未将其作为功能测试证据。

以下列出 20 项具体问题：12 项 P1、8 项 P2。P1 为发布前应优先修复的阻断或结果/稳定性问题；P2 为特定配置、功能或边界条件下的缺陷。并发问题的执行交错来自源码推演，未宣称现场复现。

## P1：优先修复

### 01. 欧姆龙 long 输出分支不能编译，也没有执行写入

位置：`WindowsFormsApplication1/FormOmron.cs:1172–1180`。

`fmt == "long"` 分支创建了 `parts`，但循环引用不在作用域内的 `vals`。相邻 int、float 分支的 vals 都是局部变量。该分支同时缺少整数数组转换和 `omronFinsNet.Write` 调用。**构建已实证失败**。

建议：按协议约定解析 Int32 数组并实际写入，检查返回值，最后更新回显。不能只删除引用 vals 的循环来消除编译错误，否则 long 输出仍然不工作。

### 02. 传图失败后继续执行，可能检测上一张图

位置：`WindowsFormsApplication1/Form1.cs:3813–3904、3913–3924、4024`。

在线路径构造 CogImage 或赋 Input 抛异常后，catch 不记录、不退出；只要 bmp 槽仍非空，后续继续 Run。Input 可能保留上一帧。离线路径 TryAssignBlockInput 返回 false 后只复位 trriger、释放 img，同样没有跳出已经进入的执行分支。

影响：格式配置不匹配、缺失 Input 或传图异常时，当前产品可能使用上一产品图像产生结果。

建议：传图成功作为执行的硬条件；失败生成本帧错误记录，按设备约定输出故障/NG，并停止本帧正常结果发送。不能仅清空触发标志后继续执行。

### 03. ToolBlock 报 Error 后仍发送、统计旧输出

位置：`WindowsFormsApplication1/Form1.cs:4024–4033、4069–4076、4229–4243、5106`。

RunStatus.Error 仅设置 `runError`；该变量只用于禁止显示记录，未阻止 IO、TCP、PLC 和统计读取 Outputs。如果脚本在更新 Output 前报错，旧 Accept 仍可作为本帧结果使用。若 Run 直接抛异常，则外层只释放位图、记日志，缺少与产品对应的失败应答。

建议：每帧先初始化无效结果，只有运行成功且输出端子校验通过才允许发布正常结果；所有通道共享同一份最终判定。故障应答规则须与 PLC 配套。

### 04. 异步任务重新读取可变输出，导致串帧、乱序及统计不一致

位置：`WindowsFormsApplication1/Form1.cs:4237–4257、4417–4426、4539–4551、5017–5057`；`WindowsFormsApplication1/Form3.cs:1636–1670`。

getrecord 把通信和统计交给 Task.Run，但任务内部才读取 `myjob.block.Outputs`。检测线程返回后下一帧可修改同一 ToolBlock。TCP changeok 又先把 aa 写到 textBox8，再让另一个任务读取 textBox8.Text，因此即使上游传入了正确快照，下游仍可能用下一次调用的文本发送。任务及锁本身也不保证按帧序发送。

统计还直接依据原始 Output，而 IO 可能依据超时处理后的 tempout1 判 NG，两者会出现不一致。并发的 oksum/ngsum 自增也不是完整的串行计数流程。

建议：在所属相机线程内生成不可变结果快照，包含帧号、方案版本、最终判定及各协议数据；按相机/连接排队串行发送、统计。界面仅显示快照，不充当通信缓冲。

### 05. RTU/FINS 以及 ModbusTCP 安全写路径会跨相机写错值

位置：`WindowsFormsApplication1/FormOmron.cs:1146–1164`；`WindowsFormsApplication1/FormModbusRtu.cs:2366–2383`；`WindowsFormsApplication1/FormModbus.cs:1422–1441、1645–1659`。

发送函数遍历所有 `[5] != "无"` 的待发相机，却使用调用参数 value 解析数据，并未读取各相机自己的 `[4]` 值。相机 A、B 并发登记待发标志后，A 的发送任务可以把 A 的值同时写到 A、B 的地址，再清掉 B 的标志。ModbusTCP 的 modbusIoLock 只锁内部遍历，WriteCameraResult 的标记更新发生在锁外，因此不能消除此问题。

适用范围：RTU 的 xie_wu、欧姆龙 xie，以及 `useXieWu=false` 的 ModbusTCP xie。ModbusTCP 默认的 XieWuWriteOne 已带 camIndex，不能将本条无条件推广至该路径。

建议：发送接口显式传入相机号、地址快照和值，只消费对应请求；避免全表扫描与共享 pending 标志。验证时让两路分别发送 111、222 到不同地址并同时触发。

### 06. 关闭、切换和保存未等待完整检测生命周期结束

位置：`WindowsFormsApplication1/Form1.cs:4019–4046、9300–9305、11030–11031、11907–11909、11960–11965、8257–8262、9158–9163`。

`_detectingCount` 只覆盖 block.Run，不覆盖此前输入图像复制、后续读取记录及异步发送/存图。关闭只等待最多 1 秒，切换只等待最多 3 秒，超时后仍继续释放缓冲或 Shutdown。ReleaseAllBmpSlots 虽跳过在途 Bitmap，紧接着的 m_pSaveImageBuf 释放却不检查在途状态，Bitmap 仍可能包装已释放内存。保存则以固定 Sleep(300) 代替等待检测线程退出。

影响：长时间检测或线程调度延迟时，可能出现使用已释放图像内存、访问已关闭 ToolBlock、保存过程中工具仍被修改。具体原生崩溃需要联机或受控压力验证。

建议：先禁止新任务、停止采集并等待回调退出，再排空/取消检测和关联结果任务，最后释放或保存；超时应拒绝本次切换/保存并告警，不能继续销毁活动对象。建立明确的资源所有权。

### 07. IO 脉冲关断依赖 UI 定时器，宽度失控且存在入队顺序竞态

位置：`WindowsFormsApplication1/Form1.cs:5741–5768、5779–5781、5829–5847`。

脉冲开启由后台任务执行，关断由 20ms WinForms Timer 检查。界面执行耗时检测、序列化或阻塞操作时，关断 Tick 无法及时处理。截止时间又在实际硬件置高前计算，队列延迟会压缩实际脉冲。

另一个确定的同步缺口是：状态改变在 _ioPulseLock 内，入队在锁外。开启线程设为高后暂停，到期线程可先把关断入队，再由开启线程把置高入队；结果可能最终为高，而软件状态已为低，后续不再关断。

建议：由单一 IO 执行器管理状态、入队顺序和定时；实际置高后开始计时。要求精确脉宽时使用相机硬件定时/PLC 输出，并验证界面卡顿、短脉冲和连续重复请求。

### 08. TCP 服务端多个连接共用接收缓冲和长度

位置：`WindowsFormsApplication1/Form3.cs:798–800、812–813、828–832`。

每个客户端创建独立 Receive 线程，但 bufferr、rr 是窗体字段。两个客户端同时接收时，另一线程可以覆盖尚未解码的数据和长度，导致命令串线、丢失或解析异常。

建议：将缓冲、长度和组包状态全部放到每个连接的局部会话对象内；连接集合的增删与发送枚举也应统一同步。用两个本地客户端分别重复发送不同命令验证。

### 09. TCP 接收把一次 Receive 当成完整命令

位置：`WindowsFormsApplication1/Form3.cs:626–638、828–842`。

直接将单次收到的字节解码后用于触发/方案字符串比较，没有持久接收缓存和报文边界。一个命令分两次收到会被当成两个命令，两个命令合并收到也不能分别识别。中文多字节文本在分段时还会受逐次解码影响。

建议：明确长度头、固定长度或结束符协议，按连接缓存并拆出完整报文；保留半包，并循环消费粘连报文。具体分隔规则需沿用现场协议。

### 10. 自定义 Modbus 串口响应未完整组帧或校验

位置：`WindowsFormsApplication1/Modbus.cs:26–71、136–140、186–190`。

ReadData 单次 Read 后按 buffer.Length 解析，而不是实际读取长度 r；expectCount 除判断是否为 8 以外没有用于收满报文。写响应不检查站号、功能码、地址/数据回显、异常响应或 CRC，读响应也未校验 CRC。首轮与后续轮匹配时提取的内容范围还不一致。

建议：采用完整事务互斥及有限超时的组帧状态机，校验长度、站号、功能码、CRC 和异常响应；或者统一使用已经引入的 Modbus 库。返回实际成功/失败，不能把收到若干字节当成写入成功。

依据：[Microsoft SerialPort.Read 文档](https://learn.microsoft.com/en-us/dotnet/api/system.io.ports.serialport.read)：读取数量可能少于请求 count，必须使用返回长度并继续组帧。

### 11. PLC 写失败被覆盖为成功，或无条件显示已发送

位置：`WindowsFormsApplication1/FormModbus.cs:1441–1448`；`WindowsFormsApplication1/FormModbusRtu.cs:2383–2395、2409–2421、2435–2447`。

ModbusTCP 批量写失败后逐寄存器降级，但忽略每个 Write 返回值，随后主动创建成功结果；即使全部降级写入失败，也显示成功。RTU xie_wu 同样忽略 Write 返回值并显示“已发送”，最后清除 pending。

建议：聚合每个写操作的实际状态，失败保持错误并通知生产逻辑；区分成功、部分成功、失败。是否重试必须考虑重复写和帧过期，不应无条件无限重试。

### 12. 像素转换失败后仍把旧/未初始化缓冲交给检测

位置：`WindowsFormsApplication1/Form1.cs:11491、11512、11753–11756、11800–11803`。

ConvertToMono8/ConvertToRGB 返回错误码，但 ImageCallBack 不检查，仍填入有效帧信息、创建 Bitmap 并提交检测。转换失败时可复用上一帧缓冲或使用新分配但未写入的内容。

建议：仅 MV_OK 且输出长度有效时发布帧；失败产生明确采集错误及设备约定的失败处理。Mono8 转换的 nDstBufferSize 还写成 W×H×3，应同时修正为真实分配容量；本次未据此单独断言发生堆溢出。

## P2：功能与边界缺陷

### 13. RGB8 直出未转 BGR，红蓝通道颠倒

位置：`WindowsFormsApplication1/Form1.cs:11509–11512、11541`。

BGR8 和 RGB8 都直接 CopyMemory，随后均按 Format24bppRgb 创建 Bitmap。该内存布局读取 BGR。非直出格式已转换 BGR，但 RGB8 直出分支遗漏。**独立 API 实测红色读成蓝色**，影响彩色检测、显示和基于 Bitmap 的存图；仅灰度设备不受此项影响。

建议：RGB8 也经转换接口转为 BGR8，或逐像素交换红蓝后再发布；同时保持帧元数据与实际缓冲一致。

### 14. Bitmap stride 未按四字节对齐

位置：`WindowsFormsApplication1/Form1.cs:11530、11541`。

stride 直接传 W 或 W×3。宽度不是 4 的倍数时不符合该构造函数约束，导致回调失败。**独立 API 实测 Mono8 宽度 5、6 均失败**。是否能由现场 ROI 配置触发取决于相机 Width 增量约束。

建议：按对齐后的 stride 分配并逐行复制，或创建 Bitmap 后通过 LockBits 按真实步长写入；不能只把 stride 参数改大而不补行填充。

依据：[Microsoft Bitmap 构造函数文档](https://learn.microsoft.com/en-us/dotnet/api/system.drawing.bitmap.-ctor)：stride 必须为 4 的倍数。

### 15. 曝光补偿公式可能把曝光放大约 254 倍

位置：`WindowsFormsApplication1/Form1.cs:4390–4399`。

表达式为 `原曝光 * (255 - buchang / 255)`。当 buchang 为 255，因子是 254；在 buchang 为 0–255 的常见亮度范围内，因子接近 254–255，最后很可能被限制到曝光上限。限幅掩盖了算式问题，并不能保证正确补偿。

建议：先明确 VPP 的 buchang 物理含义和目标公式，再验证 0、中间值、255 及异常值。不要未经确认直接把括号改成某个猜测公式。

### 16. 串口校验位读取了停止位下拉框

位置：`WindowsFormsApplication1/Form4.cs:67–73`；`WindowsFormsApplication1/Form3.cs:980–990`。

保存串口设置时 `strjiaoyan = comboBox4.SelectedIndex.ToString()`；comboBox4 是停止位，真正的校验位控件是 comboBox5。因此用户选择“无/奇/偶”不生效，改变停止位反而改变校验。

建议：从 comboBox5 读取校验位，独立验证停止位、数据位、校验位的映射并检查保存/重启恢复。

### 17. 加载缺陷模板只替换窗体字典，不替换检测工具

位置：`WindowsFormsApplication1/Form9.cs:171–180`。

加载新 CogPatInspectTool 后赋给 Inspect1 和 tools1，tools1 是窗体自己的查找字典。原 block4 或嵌套 ToolBlock.Tools 中的工具未替换，也未把新模板参数拷回原工具，因此显示“加载模板成功”后实际检测仍可能使用旧模型。

建议：保存工具的所属容器与名称，在停止检测后替换实际工具，或按 SDK 支持方式更新原工具模板。验证新模板确实改变检测结果，而非仅改变训练窗口。

### 18. 连续运行存图文件名不唯一，同一秒可能覆盖

位置：`WindowsFormsApplication1/Form1.cs:3933–3940（其余相机同构）、4493、14987–14997`。

文件名由秒级时间加 JobNumber 构成；在线路径的 numberng 在“连续运行”模式不递增。开启连续存图（cuntu==1）后，同一秒多张同判定图片会得到同名路径。锁只防并发文件对象操作，不保证每帧文件唯一。

建议：用不可重复的采集帧号和高精度采集时间组成文件名；跨方案/重启也应避免冲突，并验证一秒多帧存图数量和回查对应关系。

### 19. 曲线采样允许负间隔，异常发生在后台 catch 外

位置：`WindowsFormsApplication1/DemoControl/UserControlCurve.cs:47–56、68–75`。

只校验能否转换整数，未校验区间。输入 -2 可进入后台 Thread.Sleep(-2)，该语句位于 try 外，会产生未处理异常；-1 则无限等待，0 则高频轮询。

建议：启动线程前约束合理正整数范围，把线程整个循环纳入异常/取消处理。验证 -2、-1、0、正常间隔及停止后重新启动。

### 20. 默认高权限口令与可编辑明文配置形成薄弱权限边界

位置：`WindowsFormsApplication1/Form5.cs:58、78`。

厂家/操作工认证直接比对 INI 明文值，缺失配置时回退到代码内默认值。如果把本地登录视为阻止操作工改检测参数的权限边界，具备配置目录写权限的人可改变密码；未配置密码时继续使用默认口令。

建议：首次启用要求设定口令，保存带盐密码摘要，限制配置目录写权限，并明确哪些操作需要厂家权限。若现场设计本就允许所有本机管理员管理软件，应按该威胁边界评估，不夸大为远程漏洞。

## 其他维护观察

- Form1 同时负责采集、检测、UI、PLC、IO、方案和存储；八路大量复制代码使修复容易漏同步。优先统一结果快照、相机会话和通信发送器，再逐步抽取界面逻辑，无需一次性重写。
- 关闭 `CheckForIllegalCrossThreadCalls` 不能使控件线程安全；仍有后台更新 listBox、读取 TextBox/Grid 的路径。修复核心并发后应恢复调试检查，统一 UI 更新入口。
- 项目依赖 E 盘 VisionPro 绝对路径及 bin/Debug 中的部分 DLL，且默认 AnyCPU 配置与多项 AMD64 引用并存。本次仅验证 Debug/x64；建议固定发布平台及可重建依赖清单。
- 根目录同名源码、备份源码和已有 HTML 报告容易混淆版本。建议后续用版本控制明确实际工程来源，保留现场发布二进制与源码版本的对应关系。
- 日志、统计和存图有多处同步磁盘访问、扫描目录及每帧 Task.Run。需在正确性修复后按实际帧率和磁盘性能做长时间测量，不能仅凭静态代码宣称已测得泄漏或性能瓶颈。

## 建议修复与验收顺序

1. 修复 01，保证当前源码能编译；核对运行 EXE 对应的源码版本。
2. 优先修复 02–05、11–12，建立单帧结果快照和每路正确路由。注入传图失败、脚本错误、交替 OK/NG，核对所有通道结果一致。
3. 修复 06–07，使用完成信号管理资源与 IO。注入超过 3 秒检测、切换/关闭、UI 阻塞和 IO 请求竞争，验证无残留输出及释放后访问。
4. 修复 08–10，测试双客户端、半包/粘包、串口短帧/CRC 错误、PLC 断线及部分写失败。
5. 修复 P2 条目，覆盖 RGB/BGR/Mono、允许的 ROI 宽度、模板切换、曝光边界、串口参数和连续存图。
6. 用现场八相机及 PLC 做长时间回归，统计采集帧数、检测帧数、背压丢帧、超时、应答数量和图片数量；是否容许丢帧须按“连续观察”与“每件必检”的生产要求分别定义。

本报告列的是当前源码问题与待验证条件，不代表已经完成硬件联机验收，也不代表列出问题之外不存在其他缺陷。
