# 第四轮复审：按单一写入模式评估

## 前提与验证

接受用户明确约束：xie 与 xie_wu 不同时启用，生产只用其中一种。本报告不把两种模式同时调用、互相干扰作为任何问题的前提。ModbusTCP 与 ModbusRTU 中同名方法实现不同，也不能混为同一发送路径。

本轮重新构建当前解决方案 Debug/x64：成功，退出码0。产物在 audit-fourth，日志 audit-fourth-build.log。未改业务代码，未运行生产程序或访问相机/PLC。

对两个同步问题做了独立C#最小复现，复用当前代码的关键控制流/操作顺序，而非运行完整Form1：

- 传图失败分支后计数结果：`FailedInputCounter=1`。
- 暂停消费者于“出队后、执行计数递增前”，排空条件为：`FlushWouldReturn=True, ActionRan=False`。

这些结果验证软件控制流问题，不代表做过硬件联机复现。

## 本轮发现

### 1. [P1，新引入] 跳过 Run 时检测计数不归还

位置：WindowsFormsApplication1/Form1.cs:4025–4034、4064。

现在先 Interlocked.Increment(ref _detectingCount)，随后 if(inputAssignFailed) 跳过 Run，但 try/catch/finally 属于 else。输入失败分支根本不进入 finally，因此不会执行 Decrement。一次输入失败后计数就净增加1，正常帧的加减无法抵消这个遗留值，WaitDetectDrain 会持续超时。

这一修复确实阻止了用旧图执行Run，但引入了计数泄漏。当前超时路径仍继续关闭方案，故障后更容易反复走超时告警和不安全关闭路径。

建议：把输入失败判定放到统一 try/finally 内，保证所有已加计数的路径都减计数；或者只在实际进入被计数区域前加一，同时重新核对排空语义。不要通过超时清零计数掩盖仍在执行的任务。

### 2. [P1，修复未闭合] IO 排空存在出队到登记执行之间的空档

位置：WindowsFormsApplication1/Form1.cs:5746–5748、5780–5783。

GetConsumingEnumerable 先取出任务，下一条语句才增加 _ioWorkExecuting。最后一个任务出队后、增加计数前若线程被切走，Flush 同时看到队列为空、执行计数为0，就提前返回；此时最后的关断动作尚未执行。独立受控时序复现了这一条件。

新增执行计数缩小了旧问题的窗口，但两项状态仍不是一个完整的完成协议。即使只用一种协议，也完全独立存在。

建议：在任务发布前登记“已提交未完成”数量，在执行 finally 中扣减，入队失败回滚；或者在停止新生产者后排入完成屏障并等待屏障执行。超时应返回失败供关闭流程处理，不应只记日志后继续。单纯分别读取 Queue.Count 和 executing 不能保证已排空。

### 3. [P1，单一模式内部] 同一相机两帧仍可能争用 pending

位置：WindowsFormsApplication1/FormModbus.cs:1649–1663、1769–1784；上游 WindowsFormsApplication1/Form1.cs 的 ModbusTCP Task.Run 发送路径。

仅以启用 xie_wu 的 ModbusTCP 路径说明，不调用 xie：

1. 帧A的 WriteCameraResult 登记相机的 [5] 后暂停。
2. 帧B的 WriteCameraResult 再次登记同一相机的 [5] 后暂停。
3. A进入 XieWuWriteOne，读取 [5]，在1784将它清为“无”，随后发送A参数。
4. B进入 XieWuWriteOne，在1774发现 [5] 为“无”，直接返回，B结果没有发送。

两帧检测本身可以串行；上游发起的发送任务异步执行，不受检测线程返回后的 _detectBusy 保护，所以该交错仍可发生。1795的通信锁在清pending之后，不能保护登记/消费过程。

如果只启用 xie，仍需单独评估它的单槽登记与清理；这不是 xie 与 xie_wu 之间的干扰。若实际系统有“收到每帧应答前绝不产生下一帧”的外部约束，风险频率会不同，但当前源码本身没有保证这一点。

建议：对按相机直接发送的 XieWuWriteOne，配置映射与待发标志应分离，不必经过全表pending门禁；同时串行消费相机/连接的不可变请求以保证顺序。若业务要求每帧送达，用请求队列而不是覆盖式单槽。

### 4. [P2，部分修复] 曲线旧线程仍会在停止/重启后读一次、显示一次

位置：WindowsFormsApplication1/DemoControl/UserControlCurve.cs:79–89。

新增 generation 校验使旧线程不再长期进入下一轮循环，原持续重复轮询问题已有改善。但只在 while 入口检查，Sleep 返回后直接 ReadInt16；显示前也只检查 isThreadRun，没有核对 generation。

旧线程睡眠时停止再启动，旧线程醒来仍会完成一次旧轮次读取，并可能把旧数据追加到新一轮曲线，然后才在下一次while退出。停止后若没有重启，也仍会多读一次，只是不显示。

建议：在Sleep后、实际读之前以及UI委托真正执行时复核取消/代际；明确允许已经开始的通信怎样收尾。共享取消/代际状态的跨线程可见性也应使用合适的同步方式。

### 5. [P2，仅ModbusTCP的xie_wu模式配置string时] string 类型被静默跳过

位置：WindowsFormsApplication1/FormModbus.cs:1784、1798–1826。

XieWuWriteOne 已清pending，但类型分支只有 int、long、float；其他类型直接return。若配置为string，既不调用Write，也不到达失败日志。xie对string的实现不会帮助这里，因为按用户约束只启用一种模式。

建议：明确界面禁止此模式下不支持的类型并在加载配置时校验，或实现string发送；不可接受配置后静默清除待发请求。仅配置数值类型的现场不受此项影响。

## 仍需保留的边界

- 当前检测排空超时仍只记日志后Shutdown，保存仍依赖固定等待；不因本轮构建通过而关闭该问题。
- CSV的ji数据仍在后台读可变ToolBlock；协议参数快照不能自动解决CSV串帧。
- UI Timer控制脉冲关断、标准RTU组帧校验及自定义TCP边界问题仍应按各自启用条件处理。
- 本轮不重复展开未改的stride、模板加载和存图同名问题，历史结论不自动变成已修复。

优先修复第1项新增计数泄漏、第2项完成协议，再处理实际启用模式中的请求生命周期。第3项的全部示例只涉及xie_wu自身，多模式互斥并不能消除同模式多帧竞争。
